## There are some things i write down.


There are few of functions which is not given in properate way, even kind of ugly way. 

So i annotate them by: `// >< #ugly way.` *Have to modify them into a elegant way*